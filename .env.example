# Copy to your host's environment variables (not committed).
PORT=3000
ADMIN_USER=admin
ADMIN_PASS=change-me
# Long random string; keeps the admin logged in across restarts.
SESSION_SECRET=
# Folder for birthday.db (mount a persistent volume here in production).
DATA_DIR=./data
