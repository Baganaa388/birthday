# Original photos (the site uses the background-free versions in images/)
*.jpg
*.jpeg
*.JPG
image.png

# Local notes with the admin password
Claude.md
CLAUDE.md

# Test leftovers
_t*.html

# Node
node_modules/

# Database (guest answers stay on the server, not in git)
data/
*.db
*.db-*
.env

# Screenshots / notes
image*.png

# Site images are needed (only the original photos in the repo root are excluded)
!public/images/*.jpg
